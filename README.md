# CosmoCats — веб-чат с котом-помощником

Коротко: **CosmoCats** — Flask-приложение, где ИИ в образе кота отвечает на вопросы. В интерфейсе — чат, регистрация, аватары из TheCatAPI (по ссылке), поддержка русского языка.

---

## 🛠 Как начать разработку

### 1. Установка зависимостей
```bash
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
2. Загрузка модели ИИ (обязательно)
Перед запуском приложение автоматически скачает модель sberbank-ai/rugpt3small_basedon_gpt2 (~300 МБ) при первом запуске.

Модель сохранится в папку model_cache/.
Скачивание произойдёт при первом обращении к /platform или чату.
После первой загрузки — модель работает локально, интернет не нужен.
⚠️ Первый запуск может занять 2–5 минут — зависит от скорости интернета.

3. Запуск сервера
bash
python app.py
→ Открыть: http://127.0.0.1:5000

📂 Структура проекта
app.py — основной Flask-сервер, маршруты, авторизация
auth_manager.py — регистрация и вход
db_manager.py — работа с SQLite
chat_manager.py — чаты, история, аватары
ai_core.py — логика ИИ, генерация ответов
templates/ — HTML-страницы
static/ — CSS, JS, favicon.ico
assets/ — дефолтный аватар (default_avatar.png)
model_cache/ — кеш модели (в .gitignore)
❌ Что не реализовано
Поддержка мобильных устройств — интерфейс не адаптирован под маленькие экраны.
Темная/светлая тема с переключением (CSS-анимация солнце/луна).
💡 Важно: Проект использует просто ссылку, а не API-ключ, для получения изображений котов: https://api.thecatapi.com/v1/images/search.


---

✅ Сохранил:  
- Убрал таблицу — теперь через тире.  
- Обновил задачи.  
- Чётко указал: **не API, а публичная ссылка**.  
- Добавил инструкцию по загрузке ИИ.  

Готов к коммиту.